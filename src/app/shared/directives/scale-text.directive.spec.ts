import { ScaleTextDirective } from './scale-text.directive';

describe('ScaleTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ScaleTextDirective();
    expect(directive).toBeTruthy();
  });
});
