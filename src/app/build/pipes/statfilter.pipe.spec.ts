import { StatfilterPipe } from './statfilter.pipe';

describe('StatfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StatfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
